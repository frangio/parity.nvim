# parity.nvim

A new take on an auto-pair plugin that focuses on behaving intuitively.

- Backspace always reverts previous insertions or motions
- Supports only `()`, `[]`, and `{}`
- 300 lines of Lua
